# Arch Linux ARM's zram driver exposes lzo-rle, not the zstd backend requested
# by Omarchy's vendor configuration. Keep compressed swap enabled without a
# misleading setup warning.
[zram0]
compression-algorithm = lzo-rle
